<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>図書一覧</title>
</head>
<body>
    <h1>図書管理システム</h1>
    
    <h3>新規登録</h3>
    <form action="/books/add" method="POST">
        <input type="text" name="title" placeholder="本題" required>
        <input type="text" name="author" placeholder="著者" required>
        <button type="submit">登録</button>
    </form>
    <hr>
    <table border="1">
        <tr><th>ID</th><th>タイトル</th><th>操作</th></tr>
        <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><a href="/books/<%= row.id %>"><%= row.title %></a></td>
            <td><a href="/books/delete/<%= row.id %>">削除</a></td>
        </tr>
        <% } %>
    </table>
    <br>
    <a href="/tasks">タスク管理へ</a> | <a href="/items">商品管理へ</a>
    <hr>
    <a href="/">ポータル画面（ホーム）へ戻る</a>
</body>
</html>